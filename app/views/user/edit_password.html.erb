<%form_for (:user, :url => {:action => 'save_password', :id => params[:id]}) do |f| %>
  <%= error_messages_for 'user' %>
  Enter Password: <%= f.password_field :password %>
  Confirm : <%= f.password_field :password_confirmation %>
  <%= f.submit "Submit Change" %>
<% end %>
